# Validator 수정계획 2 — 실이미지 테스트 기반 전면 재설계

> 작성일: 2026-03-09
> 근거: `src/sample/run_validator.py` 실행 결과 (합본.png + 6개 레이어) + 설계 검토
> **⚠️ 본 계획은 Validator 구성요소 전체에 대한 전면적 수정을 포함한다.**

---

## 현상 요약 (실이미지 테스트 기준)

```
모든 오브젝트 occlusion  = 0.000  ← 버그 (hf_mobilesam.py 로직 오류)
모든 오브젝트 degree     = 4      ← 더미 (SmartLogicalAdapter 레이어 수 기반)
모든 오브젝트 hop        = 1~4    ← 더미 (SmartLogicalAdapter 인덱스 기반)
answer_obj_count         = 0      → FAIL
```

표면적으로는 "판정 기준 미충족"이지만, 실제로는 **세 층위의 문제**가 겹쳐 있다:

1. **버그**: `hf_mobilesam.py` occlusion 계산 로직 오류
2. **더미 사용**: `SmartLogicalAdapter`가 실 데이터를 무시하고 고정값 반환
3. **설계 미완성**: 시각적 유사도 기반 메트릭이 전혀 구현되지 않음

---

## 전면 재설계 범위

> 아래 표시된 파일들은 수정계획2.md §3~§5의 내용을 반영해 **모두 수정이 필요**하다.

| 파일 | 수정 필요성 |
|------|-----------|
| `hf_mobilesam.py` | occlusion 로직 교체 (버그), layer_degree 실계산 추가 |
| `hf_moondream2.py` | degree 계산을 Phase 3으로 이전 (시각 유사도 기반으로 변경) |
| `hf_yolo_clip.py` | visual_similarity, similar_count, similar_distance, object_count 추가 |
| `models/types.py` | VisualVerification, PhysicalMetadata 필드 확장, PhysicalMetadata에 alpha_degree 추가 |
| `domain/services/verification.py` | ScoringWeights 확장, resolve_answer 조건 추가, compute_difficulty 항 추가, scene_difficulty 다중 객체 가중 |
| `application/use_cases/validator/scoring.py` | 새 메트릭 반영, object_count 기반 answer_obj_count |
| `adapters/outbound/models/dummy.py` | DummyVisualVerification 새 필드 추가 |
| `src/sample/test_samples.py` | SmartLogicalAdapter 더미 제거, SmartVisualAdapter dummy 명시 |
| `src/sample/run_validator.py` | SmartLogicalAdapter → physical 실데이터 재활용 |
| `conf/validator.yaml` | 새 weight 파라미터 추가 |

---

## §1 즉시 수정 — 버그 (현재 실행 불가 원인)

### 1-1 🔴 `hf_mobilesam.py` occlusion = 0.000 버그

**위치**: [hf_mobilesam.py:75-79](../../src/discoverex/adapters/outbound/models/hf_mobilesam.py#L75-L79)

**문제**: 합본이 불투명 RGBA이면 `comp_alpha = 전체 True` → `occlusion = 0` 항상

```python
# 현재 (잘못됨)
comp_alpha = composite[:, :, 3] > 0
visible    = alpha & comp_alpha      # comp_alpha가 전체 True면 = alpha 그 자체
occlusion  = 1 - visible.sum() / total_pixels  # = 0

# 수정 (레이어-간 직접 비교, CpuPhysicalAdapter와 동일)
covered = np.zeros((H, W), dtype=bool)
for j in range(i + 1, n):           # j = 나보다 위에 있는 레이어
    covered |= layer_arrays[j][:, :, 3] > 0
occlusion = int((alpha_i & covered).sum()) / total_pixels
```

### 1-2 🔴 `SmartLogicalAdapter` — hop/degree 더미 제거

**위치**: [test_samples.py:239-253](../../src/sample/test_samples.py#L239-L253)

현재 `hop_map = {oid: min(i+1, 4)}` — 인덱스 순서 고정값.
`CpuPhysicalAdapter`가 이미 BFS로 `z_depth_hop_map`을 실계산하므로 재활용한다.
또한 alpha-overlap 그래프의 degree도 `CpuPhysicalAdapter` 내부에서 이미 계산 가능하다.

```python
# 수정안
class SmartLogicalAdapter:
    def __init__(self, physical_adapter: CpuPhysicalAdapter) -> None:
        self._physical = physical_adapter

    def extract(self, composite_image, physical: PhysicalMetadata) -> LogicalStructure:
        obj_ids = sorted(physical.occlusion_map.keys())
        # BFS hop 실계산값 재활용
        hop_map  = {oid: physical.z_depth_hop_map.get(oid, 0) for oid in obj_ids}
        # alpha-overlap graph degree 실계산 (physical adapter에 추가 필요)
        degree_map = self._physical.last_alpha_degree_map or {oid: 0 for oid in obj_ids}
        diameter = float(max(hop_map.values(), default=1))
        return LogicalStructure(
            relations=[],
            degree_map=degree_map,
            hop_map=hop_map,
            diameter=max(diameter, 2.0),
        )
```

`CpuPhysicalAdapter`에 `last_alpha_degree_map: dict[str, int]` 속성 추가 필요.

---

## §2 전면 재설계 — 판정 조건 재정의

### 현재 `resolve_answer` 조건 (5개 중 2개)

```
1. occlusion_ratio > 0.3        → 물리적 피가림
2. sigma_threshold <= 4         → 블러 내성
3. degree >= 3                  → 연결 차수 (물리 경계 기준)
4. z_depth_hop >= 2             → 매몰 깊이
5. neighbor_count >= 3          → 공간 밀집도
```

### 목표 조건 (재정의)

| # | 조건 | 분류 | 현재 상태 |
|---|------|------|----------|
| 1 | `occlusion_ratio > θ` | 물리 | ✅ (버그 수정 필요) |
| 2 | `drr_slope >= θ` | 시각 | ✅ (수식에 반영, resolve_answer에 미추가) |
| 3 | `cluster_density >= θ` | 공간 | ✅ (`neighbor_count`) |
| 4 | `z_depth_hop >= θ` | 논리 | ✅ |
| 5 | `visual_degree >= θ` | 시각+논리 | ❌ **미구현** — 유사 특징 기반 엣지 수 |
| 6 | `similar_count >= θ` | 시각 | ❌ **미구현** — 유사한 객체가 여러 개 존재 |
| 7 | `similar_distance <= θ` | 시각+공간 | ❌ **미구현** — 유사 객체까지 평균 거리 (가까울수록 찾기 어려움) |

**resolve_answer 수정안**: 7개 조건 중 N개 이상 (N은 YAML 파라미터화)

---

## §3 Phase별 상세 변경

### Phase 1 — `hf_mobilesam.py`

| 항목 | 현재 | 수정 후 |
|------|------|--------|
| occlusion | composite alpha 비교 ❌ | 레이어-간 z_order 비교 ✅ |
| alpha_degree | 없음 | BFS 그래프의 node degree 추출 → `PhysicalMetadata.alpha_degree_map` 추가 |

`alpha_degree_map: dict[str, int]` — alpha overlap 그래프 기준 연결 차수.
**이 값은 물리적 엣지 수이며**, §3 Phase 3의 `visual_degree_map`과 구분된다.

---

### Phase 2 — `hf_moondream2.py`

| 항목 | 현재 | 수정 후 |
|------|------|--------|
| degree_map | Moondream2 scene graph 기반 | **삭제** — Phase 3 CLIP 기반 visual_degree로 대체 |
| hop_map | NetworkX BFS | 유지 |
| diameter | NetworkX | 유지 |

> **설계 결정**: `degree_map`은 "시각적으로 유사한 객체와의 엣지 수"로 재정의되므로,
> CLIP feature가 있는 Phase 3에서 계산해야 한다.
> Phase 2는 hop/diameter 전담으로 역할을 축소한다.

---

### Phase 3 — `hf_yolo_clip.py` ← **가장 큰 변경**

**추가할 출력 메트릭:**

| 메트릭 | 타입 | 설명 |
|--------|------|------|
| `visual_degree_map` | `dict[str, int]` | CLIP 유사도 > 임계값인 다른 객체 수 → degree 대체 |
| `similar_count_map` | `dict[str, int]` | 유사도 임계값 초과 객체 수 (`visual_degree`와 동일, 별도 임계값 가능) |
| `similar_distance_map` | `dict[str, float]` | 유사 객체들까지의 평균 유클리드 거리 (center 좌표 기준) |
| `object_count_map` | `dict[str, int]` | 레이어당 YOLO 탐지 객체 수 (1 레이어 → N object) |

**계산 흐름:**

```python
# 1. 각 레이어별 CLIP feature 추출 (기존과 동일, bbox-crop 기준)
clip_features: dict[str, Tensor]  # {obj_id: feature_vector}

# 2. 레이어 center 좌표 (physical.regions 에서 읽어옴)
centers: dict[str, tuple[float, float]]

# 3. 객체 간 CLIP 코사인 유사도 행렬
similarity_matrix: dict[str, dict[str, float]]
for i in obj_ids:
    for j in obj_ids:
        if i != j:
            sim = cosine_similarity(clip_features[i], clip_features[j])

# 4. visual_degree: 유사도 > SIMILARITY_THRESHOLD 인 객체 수
SIMILARITY_THRESHOLD = 0.75  # YAML 파라미터화 대상
visual_degree_map[i] = sum(1 for j in obj_ids if j != i and sim(i,j) > THRESHOLD)

# 5. similar_distance: 유사 객체들까지 평균 유클리드 거리
similar_peers = [j for j in obj_ids if j != i and sim(i,j) > THRESHOLD]
similar_distance_map[i] = mean([dist(center_i, center_j) for j in similar_peers])
#   → 유사한 객체가 가까이 있을수록 찾기 어려움 = 낮은 값 = 높은 난이도

# 6. object_count: 레이어 PNG에 YOLO로 다중 객체 탐지
object_count_map[obj_id] = len(yolo.detect(layer_image))  # 레이어별 탐지 수
```

**`VisualVerification` 타입 확장** ([types.py](../../src/discoverex/models/types.py)):

```python
class VisualVerification(BaseModel):
    sigma_threshold_map: dict[str, float] = {}
    drr_slope_map: dict[str, float] = {}
    visual_degree_map: dict[str, int] = {}       # ← 신규
    similar_count_map: dict[str, int] = {}        # ← 신규
    similar_distance_map: dict[str, float] = {}   # ← 신규
    object_count_map: dict[str, int] = {}         # ← 신규 (레이어당 YOLO 탐지 수)
```

**`PhysicalMetadata` 타입 확장** ([types.py](../../src/discoverex/models/types.py)):

```python
class PhysicalMetadata(BaseModel):
    ...
    alpha_degree_map: dict[str, int] = {}  # ← 신규 (alpha overlap 그래프 degree)
```

---

### Phase 4 — `scoring.py` + `verification.py`

#### 4-1 `obj_metrics` 딕셔너리 확장

```python
metrics = {
    ...기존 키...,
    "visual_degree":    visual.visual_degree_map.get(obj_id, 0),    # ← 신규
    "similar_count":    visual.similar_count_map.get(obj_id, 0),    # ← 신규
    "similar_distance": visual.similar_distance_map.get(obj_id, inf), # ← 신규
    "alpha_degree":     physical.alpha_degree_map.get(obj_id, 0),   # ← 신규
}
```

#### 4-2 `resolve_answer` 조건 확장

```python
# 수정안 (7개 조건 중 N개 이상, N은 is_hidden_min_conditions YAML)
conditions = [
    metrics.get("occlusion_ratio", 0) > 0.3,
    metrics.get("drr_slope", 0) >= 0.05,                  # ← 신규
    metrics.get("cluster_density", 0) >= 3,
    metrics.get("z_depth_hop", 0) >= 2,
    metrics.get("visual_degree", 0) >= 2,                 # ← 신규 (유사 객체 수)
    metrics.get("similar_count", 0) >= 1,                 # ← 신규
    metrics.get("similar_distance", inf) <= DIST_THRESH,  # ← 신규 (가까운 유사 객체)
]
```

#### 4-3 `compute_difficulty` 항 추가

```python
# 수정안 — D(obj) 수식에 추가
D(obj) = 기존 항들
        + w_sim_cnt  · similar_count            # 유사 객체 수 (많을수록 어려움)
        + w_sim_dist · (1 / (similar_distance + ε))  # 유사 객체가 가까울수록 어려움
```

`ScoringWeights`에 신규 필드 추가 필요:
```python
difficulty_similar_count: float = 0.15
difficulty_similar_dist:  float = 0.10
```

#### 4-4 `scene_difficulty` — 다중 객체 반영

```python
# 현재: 단순 평균
scene_difficulty = mean(D(obj) for obj in answer_objs)

# 수정안: object_count 가중 평균
# 레이어당 탐지 수가 많을수록 그 레이어의 D(obj)를 높게 반영
def compute_scene_difficulty(answer_objs, object_count_map, weights):
    weighted_sum = 0.0
    total_weight = 0
    for obj in answer_objs:
        count = object_count_map.get(obj["obj_id"], 1)
        weighted_sum += compute_difficulty(obj, weights) * count
        total_weight += count
    return weighted_sum / total_weight if total_weight > 0 else 0.0
```

#### 4-5 `answer_obj_count` — 레이어당 탐지 수 합산

```python
# 현재: resolve_answer 통과 레이어 수
answer_obj_count = len(answer_obj_metrics)

# 수정안: 통과 레이어의 YOLO 탐지 객체 수 합
answer_obj_count = sum(
    visual.object_count_map.get(obj["obj_id"], 1)
    for obj in answer_obj_metrics
)
```

---

## §4 run_validator.py 개선 (더미 제거 가능 범위)

### GPU 없이 CPU에서 실계산 가능한 항목

| 메트릭 | 현재 | 개선 후 |
|--------|------|--------|
| `hop_map` | SmartDummy (인덱스 순서) | `physical.z_depth_hop_map` 직접 재활용 ✅ |
| `degree_map` | SmartDummy (레이어 수 기반) | `physical.alpha_degree_map` (alpha overlap 그래프 degree) ✅ |
| `diameter` | SmartDummy (레이어 수) | BFS max path from physical graph ✅ |
| `sigma_threshold` | 고정 8.0 | ❌ CLIP 없이 계산 불가, 더미 유지 |
| `drr_slope` | 고정 0.15 | ❌ CLIP 없이 계산 불가, 더미 유지 |
| `visual_degree` | 미구현 | ❌ CLIP 없이 계산 불가, 0으로 표기 |
| `similar_count` | 미구현 | ❌ CLIP 없이 계산 불가, 0으로 표기 |
| `similar_distance` | 미구현 | ❌ CLIP 없이 계산 불가, inf로 표기 |
| `object_count` | 미구현 | ❌ YOLO 없이 계산 불가, 1로 표기 |

### 개선 후 `run_validator.py` 출력 신뢰도

```
Phase 1 결과 (실계산):  occlusion ✅  z_depth_hop ✅  cluster_density ✅
Phase 2 결과 (실계산):  hop ✅  degree (alpha) ✅  diameter ✅
Phase 3 결과 (더미):    sigma ❌  drr_slope ❌  visual_degree ❌  similar_count ❌
Phase 4 결과:           occlusion+hop+degree(alpha) 기반 판정 — 시각 조건 미반영
```

**결론**: CPU 환경에서는 물리·논리 조건만 실계산 가능. 시각적 유사도 조건은 GPU 필요.
출력 시 각 메트릭의 신뢰도(실계산/더미)를 명시하도록 표기 추가.

---

## §5 수정 순서 및 의존성

```
STEP 1 (즉시) ──────────────────────────────────────────────
  1a. hf_mobilesam.py occlusion 로직 교체 + alpha_degree_map 추출
  1b. PhysicalMetadata.alpha_degree_map 필드 추가 (types.py)
  1c. test_samples.py SmartLogicalAdapter: physical 데이터 재활용
  1d. run_validator.py: SmartLogicalAdapter(physical_adapter=phys) 전달

STEP 2 (단기) ──────────────────────────────────────────────
  2a. VisualVerification 타입 확장
      (visual_degree_map, similar_count_map, similar_distance_map, object_count_map)
  2b. dummy.py DummyVisualVerification 새 필드 기본값 추가
  2c. hf_yolo_clip.py: visual_similarity 행렬 계산 + 4개 신규 메트릭 추가
  2d. hf_moondream2.py: degree_map 계산 제거 → hop/diameter 전담으로 축소
  2e. hf_mobilesam.py: degree_map 계산 추가 (alpha_degree_map, Phase 2에서 이전)

STEP 3 (단기) ──────────────────────────────────────────────
  3a. scoring.py: obj_metrics에 신규 키 추가
  3b. verification.py: resolve_answer 조건 확장 (7개 조건)
  3c. verification.py: ScoringWeights 신규 필드 추가
  3d. verification.py: compute_difficulty 항 추가
  3e. verification.py: compute_scene_difficulty object_count 가중 추가
  3f. scoring.py: answer_obj_count → object_count_map 합산으로 변경

STEP 4 (단기) ──────────────────────────────────────────────
  4a. 전체 테스트 업데이트 (test_validator_scoring.py, test_validator_e2e.py)
  4b. run_validator.py 출력에 실계산/더미 신뢰도 표기 추가
  4c. 동일 씬 재실행 → 수치 확인

STEP 5 (중기) ──────────────────────────────────────────────
  5a. resolve_answer 임계값 튜닝 (실측값 분포 기반)
  5b. is_hidden_min_conditions YAML 연결 (하드코딩 2 → YAML)
  5c. SIMILARITY_THRESHOLD YAML 파라미터화
  5d. conf/validator.yaml 신규 weight 항목 추가
  5e. drr_slope 판정 임계값 결정
      — §4-2의 `drr_slope >= 0.05`는 임시값. GPU 환경에서 실측 씬 분포를 수집한 후
        확정. YAML 파라미터화 대상 (conf/validator.yaml drr_slope_threshold).
  5f. D(obj) 수식 가중치 재조정
      — 신규 항(difficulty_similar_count=0.15, difficulty_similar_dist=0.10) 추가 시
        기존 가중치 합 1.0이 1.25로 초과됨.
        해결 방안: 기존 5개 항의 가중치를 0.8 비례 축소하거나,
        compute_difficulty 내부에서 합산 후 정규화(÷총합) 방식 적용.
        정규화 방식 채택 시 ScoringWeights 수치 변경 없이 compute_difficulty만 수정.

STEP 6 (장기, GPU 필요) ────────────────────────────────────
  6a. GPU 환경에서 Phase 3 전체 실측
  6b. visual_degree / similar_count 기반 is_hidden 판정 검증
  6c. WeightFitter 재학습 (신규 메트릭 포함)
```

---

## §6 현재 `구현현황.md` 잔여 과제 갱신 대상

| 과제 | 우선순위 |
|------|---------|
| `hf_mobilesam.py` occlusion 로직 교체 + alpha_degree_map | **즉시** |
| `PhysicalMetadata` alpha_degree_map 필드 추가 | **즉시** |
| `SmartLogicalAdapter` 더미 제거 → physical 실데이터 재활용 | **즉시** |
| `VisualVerification` 타입 확장 (4개 신규 필드) | **단기** |
| `hf_yolo_clip.py` 시각 유사도 메트릭 추가 | **단기** |
| `hf_moondream2.py` degree 계산 제거 | **단기** |
| `resolve_answer` 조건 7개로 확장 | **단기** |
| `ScoringWeights` 신규 weight 추가 | **단기** |
| `compute_scene_difficulty` object_count 가중 반영 | **단기** |
| `is_hidden_min_conditions` YAML 연결 | 중간 → **높음** |
| 샘플 테스트 재실행 | 중간 → **높음** (STEP 1 완료 후 즉시) |
| `bundle_to_jsonl.py` 작성 | 높음 유지 |

---

## 참고 — 변경 전후 데이터 플로우

```
변경 전:
  Phase 1  →  PhysicalMetadata (occlusion❌, hop, cluster, euclidean)
  Phase 2  →  LogicalStructure (degree=alpha_overlap, hop=BFS, diameter)
  Phase 3  →  VisualVerification (sigma, drr_slope)
  Phase 4  →  VerificationBundle (5개 조건 판정)

변경 후:
  Phase 1  →  PhysicalMetadata (occlusion✅, hop, cluster, euclidean, alpha_degree[신규])
  Phase 2  →  LogicalStructure (degree 제거, hop=BFS, diameter)
  Phase 3  →  VisualVerification (sigma, drr_slope, visual_degree[신규],
                                  similar_count[신규], similar_distance[신규],
                                  object_count[신규])
  Phase 4  →  VerificationBundle (7개 조건 판정, object_count 가중 난이도)
```