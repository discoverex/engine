# @package _global_
defaults:
  - override /models/hidden_region: hf_cpu_fast
  - override /models/inpaint: hf_cpu_fast
  - override /models/perception: hf_cpu_fast
  - override /models/fx: tiny_sd_cpu
  - override /runtime/model_runtime: cpu

runtime:
  width: 320
  height: 240
