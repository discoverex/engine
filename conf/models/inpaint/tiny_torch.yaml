# @package models.inpaint
_target_: discoverex.adapters.outbound.models.tiny_torch_inpaint.TinyTorchInpaintModel
model_id: tiny-torch-inpaint
device: ${runtime.model_runtime.device}
dtype: ${runtime.model_runtime.dtype}
seed: ${runtime.model_runtime.seed}
strict_runtime: true
