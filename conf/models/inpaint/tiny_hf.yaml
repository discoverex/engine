# @package models.inpaint
_target_: discoverex.adapters.outbound.models.tiny_hf_inpaint.TinyHFInpaintModel
model_id: tiny-hf-inpaint
device: ${runtime.model_runtime.device}
dtype: ${runtime.model_runtime.dtype}
seed: ${runtime.model_runtime.seed}
strict_runtime: true
