# @package models.fx
_target_: discoverex.adapters.outbound.models.tiny_hf_fx.TinyHFFxModel
model_id: tiny-hf-fx
device: ${runtime.model_runtime.device}
dtype: ${runtime.model_runtime.dtype}
seed: ${runtime.model_runtime.seed}
strict_runtime: true
