# @package models.perception
_target_: discoverex.adapters.outbound.models.tiny_torch_perception.TinyTorchPerceptionModel
model_id: tiny-torch-perception
device: ${runtime.model_runtime.device}
dtype: ${runtime.model_runtime.dtype}
seed: ${runtime.model_runtime.seed}
strict_runtime: true
