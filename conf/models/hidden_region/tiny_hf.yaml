# @package models.hidden_region
_target_: discoverex.adapters.outbound.models.tiny_hf_hidden_region.TinyHFHiddenRegionModel
model_id: tiny-hf-hidden
device: ${runtime.model_runtime.device}
dtype: ${runtime.model_runtime.dtype}
seed: ${runtime.model_runtime.seed}
strict_runtime: true
